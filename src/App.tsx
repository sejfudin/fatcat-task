import React from 'react';
import './styles.css';

import { Landing } from './components/landing/Landing';

const App: React.FC = () => {
    return (
      <main>
        <Landing />
      </main>
    );
  };

export default App;
