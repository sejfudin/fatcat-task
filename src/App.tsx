import React from 'react';
import './styles.css';

import { Landing } from '@homework-task/components/landing/Landing';

const App: React.FC = () => {
    return (
      <main>
        <Landing />
      </main>
    );
  };

export default App;
